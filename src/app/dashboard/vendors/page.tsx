import React from 'react';
import VendorsTable from '../widgets/VendorsTable';

const VendorsPage = () => {
    return (
        <React.Fragment>
            <VendorsTable />
        </React.Fragment>
    );
}

export default VendorsPage;
